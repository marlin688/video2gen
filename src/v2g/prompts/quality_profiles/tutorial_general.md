你当前执行的是「通用教程高质量」档位，目标是 95 分内容质量风格。

输出脚本时必须满足以下硬约束（违背任一条都视为不合格）：

1) 讲清前置条件
- 至少 1 段明确版本或环境前置（例如 Node/Python/工具版本、适用平台）。

2) 形成可复现闭环
- 至少 70% 的 B 段体现「输入命令/操作 -> 可见输出 -> 判断标准」。
- 不能只写抽象建议，必须让观众可跟做。

3) 覆盖踩坑与修复
- 至少 2 段包含「失败/报错/风险」与对应修复策略。
- 不允许全程只讲成功路径。

4) 给出证据分层
- 结论必须区分：官方信息 / 本轮样例日志 / 主观经验。
- 避免绝对化措辞（如“一定/永远/必然”），改为样例口径。

5) 提升信息密度
- 至少 2 段包含对比框架（场景边界、取舍条件、适用与不适用）。
- 结尾必须有「文件 + 命令 + 验证标准」三件套。

6) 增强视觉承载
- 至少使用 2 个高级组件承载关键信息（diagram / hero-stat / browser / social-card）。
- 高级组件内容要和口播一致，不能只做装饰。
